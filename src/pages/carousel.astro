---
import Layout from "../layouts/Layout.astro";
const sectionClassName =
  "flex w-full max-w-3xl flex-col items-center gap-10 text-center";
const noteClassName = "text-sm text-slate-700";
---

<Layout
  title="Carousel | Frontendisti Accessibility"
  description="Review modern scroll-linked carousels using scroll markers, snap behavior, anchor-positioned controls, and keyboard navigation."
>
  <div class="flex items-center justify-center h-full">
    <section class={sectionClassName}>
      <header class="space-y-4 text-center mx-auto">
        <h1 class="text-4xl font-semibold text-gray-900 mx-auto">
          Carousel Pattern Playground
        </h1>
        <p class="text-lg text-gray-800">
          Modern scroll-linked carousel using scroll markers and
          anchor-positioning buttons.
        </p>
      </header>

      <div class="carousel-demo mx-auto w-full max-w-[500px]">
        <div
          class="carousel"
          role="group"
          aria-roledescription="carousel"
          aria-label="Pet photo carousel"
        >
          <div
            role="group"
            aria-roledescription="slide"
            aria-label="Slide 1 of 3: playful dog looking toward the camera"
          >
            <img
              src="https://placedog.net/500/500"
              alt="Playful dog looking toward the camera"
            />
          </div>
          <div
            role="group"
            aria-roledescription="slide"
            aria-label="Slide 2 of 3: resting cat licking the pillow"
          >
            <img
              src="https://placecats.com/500/500"
              alt="Resting cat licking the pillow"
              loading="lazy"
            />
          </div>
          <div
            role="group"
            aria-roledescription="slide"
            aria-label="Slide 3 of 3: bear in water"
          >
            <img
              src="https://placebear.com/500/500"
              alt="Bear in water"
              loading="lazy"
            />
          </div>
        </div>
      </div>
      <p class={noteClassName}>
        Demonstrates experimental scroll-marker and scroll-button behaviours.
        Browsers without support fall back to horizontal scrolling.
      </p>
    </section>
  </div>

  <style>
    .carousel-demo {
      container-type: inline-size;
    }

    .carousel-demo * {
      box-sizing: border-box;
    }

    .carousel {
      display: grid;
      scroll-snap-type: x mandatory;
      grid-template-columns: repeat(3, 100cqi);
      overflow: auto hidden;
      anchor-name: --carousel;
      column-gap: 1rem;
      border-radius: 1.25rem;
      scroll-marker-group: after;
      scroll-behavior: smooth;
      counter-reset: snap-counter 0;
      counter-increment: snap-counter;

      &::-webkit-scrollbar {
        height: 0 !important;
        width: 0 !important;
      }
    }

    .carousel::scroll-marker-group {
      display: flex;
      block-size: 1.5rem;
      gap: 1rem;
      justify-content: center;
      margin-block-start: 1rem;
    }

    .carousel::scroll-button(*) {
      display: grid;
      position: absolute;
      position-anchor: --carousel;
      border-radius: 100vmax;
      color: white;
      font-weight: bold;
      font-size: 1.5rem;
      line-height: 1;
      text-box: trim-both ex alphabetic;
      aspect-ratio: 1;
      inline-size: 2.25rem;
      border: 2px solid var(--color-primary);
      background-color: var(--button-bg, var(--color-primary));
      place-items: center;
      text-decoration: none;
      transition:
        translate 0.25s ease-out,
        opacity 0.25s ease-out,
        scale 0.25s ease-out,
        background-color 0.25s ease-out,
        border-color 0.25s ease-out;
      cursor: pointer;
      scale: 1;
      text-decoration: none;
    }

    .carousel::scroll-button(*):disabled {
      opacity: 0;
      translate: 0;
    }

    .carousel::scroll-button(left) {
      content: "‹";
      position-area: center left;
      translate: calc(100% + 0.5rem) 0;
    }

    .carousel::scroll-button(right) {
      content: "›";
      position-area: center right;
      translate: calc(-100% - 0.5rem) 0;
    }

    .carousel::scroll-button(*):hover {
      --button-bg: color-mix(in oklch, var(--color-primary) 85%, white 15%);
      border-color: color-mix(in oklch, var(--color-primary) 85%, white 15%);
      scale: 1.1;
    }


    .carousel > div {
      container-type: scroll-state size;
      display: grid;
      place-items: center;
      counter-increment: snap-counter;
      block-size: clamp(16rem, 50svb, 22rem);
      border-radius: 1rem;
      overflow: hidden;
      scroll-snap-stop: always;
      scroll-snap-align: center;
    }

    .carousel > div::scroll-marker {
      content: counter(snap-counter) / attr(aria-label);
      background-color: var(--marker-bg, var(--color-primary));
      inline-size: 1.5rem;
      aspect-ratio: 1;
      display: grid;
      text-decoration: none;
      place-items: center;
      color: white;
      font-weight: bold;
      font-size: small;
      box-sizing: border-box;
      border-radius: 100vmax;
      scale: 1;
      transition:
        scale 0.25s ease-out,
        background-color 0.25s ease-out,
        border-color 0.25s ease-out;
      border: 2px solid var(--color-primary);
    }

    .carousel > div::scroll-marker:target-current {
      --marker-bg: color-mix(in oklch, var(--color-primary) 80%, black 20%);
      scale: 1.2;
    }

    .carousel > div::scroll-marker:hover {
      --marker-bg: color-mix(in oklch, var(--color-primary) 85%, white 15%);
      border-color: color-mix(in oklch, var(--color-primary) 85%, white 15%);
    }


    .carousel > div::scroll-marker:target-before,
    .carousel > div::scroll-marker:target-after {
      scale: 0.8;
    }

    .carousel > div img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    @supports not (scroll-marker-group: after) {
      .carousel::scroll-marker-group,
      .carousel::scroll-button(left),
      .carousel::scroll-button(right) {
        display: none;
      }
    }
  </style>
</Layout>
